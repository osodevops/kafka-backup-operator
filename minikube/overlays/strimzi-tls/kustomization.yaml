apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: kafka
resources:
  - ../../base/strimzi-kafka
  - producer.yaml
  - backup-pvc.yaml
  - kafka-backup.yaml
  # kafka-restore.yaml is applied manually after backup completes
